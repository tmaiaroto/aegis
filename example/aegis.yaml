app:
  name: Example Aegis App
  keepBuildFiles: true
aws:
  region: us-east-1
lambda:
  description: Foo does foo
  functionName: foo
  # sourceZip: Archive.zip 
  # ^ works. so it's something in the go app.
  # permissions on the aegis_app file in AWS were 33204 = 100664 (664)
  # rw-rw-r--  booooo
  # 
  # role: arn:aws:iam::12345:role/aegis_lambda_function
api:
  name: Foo API
  description: An example API
  # stages:
  #   prod:
  #     name: prod
  #     variables:
  #       foo: Bar